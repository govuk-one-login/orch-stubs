{
  "name": "orch-stubs",
  "version": "1.0.0",
  "description": "Performance and scaling stubs owned by Orchestration",
  "author": "",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "(trap 'kill 0' INT; npm run build:auth & npm run build:ipv & npm run build:spot & wait)",
    "build:auth": "sam build --template auth-stub-template.yml --build-dir build/auth",
    "build:ipv": "sam build --template ipv-stub-template.yml --build-dir build/ipv",
    "build:ipv:watch": "nodemon --watch './src/main/ipv-stub/*' --ext 'ts' --exec 'npm run build:ipv -- --cached' ",
    "build:spot": "sam build --template spot-stub-template.yml --build-dir build/spot",
    "clean": "rm -rf build",
    "clean:auth": "rm -rf build/auth & rm -rf build/build.toml",
    "clean:ipv": "rm -rf build/ipv & rm -rf build/build.toml",
    "start:local": "trap '' PIPE\n{\n sam local start-api --template build/ipv/template.yaml --env-vars parameters.json -p 3000 <&3 >&4 | sam local start-api --template build/auth/template.yaml --env-vars parameters.json -p 3001 <&3 >&4 \n} 3<&0 4>&1",
    "start:local:auth": "sam local start-api --template build/auth/template.yaml --env-vars parameters.json -p 3001",
    "start:local:ipv": "sam local start-api --template build/ipv/template.yaml --env-vars parameters.json -p 3000",
    "start:local:warm": "trap '' PIPE\n{\n sam local start-api --warm-containers EAGER --template build/ipv/template.yaml --env-vars parameters.json -p 3000 <&3 >&4 | sam local start-api --warm-containers EAGER --template build/auth/template.yaml --env-vars parameters.json -p 3001 <&3 >&4 \n} 3<&0 4>&1",
    "check": "npm run check:lint && npm run check:prettier",
    "check:lint": "eslint . --max-warnings=0",
    "check:prettier": "prettier --check \"**/*.{ts,json}\"",
    "fix": "npm run fix:lint && npm run fix:prettier",
    "fix:lint": "eslint . --fix",
    "fix:prettier": "prettier --write \"**/*.{ts,json}\"",
    "test": "jest --passWithNoTests",
    "test:coverage": "jest --coverage --passWithNoTests",
    "test:localstack:integration": "npm run localstack:up && (jest --config=./jest.config.integration.ts && e=0) || e=1 && npm run localstack:down && exit $e",
    "test:integration": "jest --config=./jest.config.integration.ts",
    "test:ci": "tsc --noEmit && npm run localstack:up && (jest --config=./jest.config.ci.ts && e=0) || e=1 && npm run localstack:down && exit $e",
    "localstack:up": "docker compose -f docker-compose.local.yml up -d",
    "localstack:down": "docker compose -f docker-compose.local.yml down",
    "prepare": "husky",
    "createRequestObject": "node scripts/createIpvRequestObject.mjs"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": true
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.775.0",
    "@aws-sdk/client-sqs": "^3.682.0",
    "@aws-sdk/lib-dynamodb": "^3.713.0",
    "@types/node-jose": "^1.1.13",
    "esbuild": "^0.25.0",
    "jose": "^5.9.6",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.10",
    "@types/supertest": "^6.0.2",
    "eslint": "^9.13.0",
    "eslint-plugin-jest": "^28.8.3",
    "globals": "^15.13.0",
    "husky": "^9.1.6",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.18.1"
  }
}
